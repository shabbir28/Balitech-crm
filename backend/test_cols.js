const db = require('./src/config/db'); async function run() { const tables = ['leads', 'van_data', 'refine_data', 'premium_data']; for (const t of tables) { const res = await db.query('SELECT column_name FROM information_schema.columns WHERE table_name = $1 AND column_name = $2', [t, 'campaign_type']); console.log(t, res.rows.length > 0 ? 'HAS campaign_type' : 'NO campaign_type'); } process.exit(0); } run();
